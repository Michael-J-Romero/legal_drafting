import PlanView from '../../components/planning/PlanView';

/**
 * Demo page to showcase the planning agent plan view components
 */
export default function PlanDemoPage() {
  return (
    <div style={{ height: '100vh', display: 'flex', flexDirection: 'column' }}>
      <PlanView />
    </div>
  );
}
